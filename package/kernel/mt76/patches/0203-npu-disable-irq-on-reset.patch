--- a/mt7996/dma.c
+++ b/mt7996/dma.c
@@ -560,6 +560,7 @@ mt76_dma_dequeue(struct mt76_dev *dev, s
 		dev_info(dev->mt76.dev,"%s L1 SER wed dma reset done.",
 			 wiphy_name(dev->mt76.hw->wiphy));
 
+	mt76_npu_disable_irqs(&dev->mt76);
 	mt7996_dma_disable(dev, force);
 
 	if (!force)
