--- a/dma.c
+++ b/dma.c
@@ -560,7 +560,7 @@ mt76_dma_dequeue(struct mt76_dev *dev, s
 
 	if (mt76_queue_is_wed_rro_data(q) ||
 	    mt76_queue_is_wed_rro_msdu_pg(q))
-		goto done;
+		return NULL;
 
 	if (mt76_queue_is_wed_rro_ind(q)) {
 		struct mt76_wed_rro_ind *cmd;
